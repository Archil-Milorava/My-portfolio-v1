import { RingLoader } from "react-spinners";

const Spinner = () => {
  return (
    <div className="flex justify-center items-center h-screen">
      <RingLoader color="#284135" />
    </div>
  );
};

export default Spinner;


